// Export all helper modules
import {
  formatDateRange,
  truncateText,
  capitalizeFirstLetter,
  generateId,
  isExternalUrl,
  scrollToSection
} from './utils';

import {
  techStackIcons,
  aboutIcons,
  projectIconMap,
  getIconByName
} from './icon-mapping';

export {
  // Utils
  formatDateRange,
  truncateText,
  capitalizeFirstLetter,
  generateId,
  isExternalUrl,
  scrollToSection,
  
  // Icon mapping
  techStackIcons,
  aboutIcons,
  projectIconMap,
  getIconByName
};